```JavaScript

/**
 * 游戏数据导出结构定义
 * 版本: 1.0.0
 */
const gameData = {
  /** 数据结构的版本号 */
  version: "1.0.0",

  /** 数据类型（"complete" 表示完整数据导出） */
  dataType: "complete",

  /** 数据导出的具体时间 */
  exportDate: new Date().toISOString(),

  /** 角色总数量（当前共 10 个角色） */
  characterCount: 10,

  /** 分组总数量（共 5 个分组） */
  groupCount: 5,

  /** 是否开启自动刷新功能（布尔值） */
  autoRefreshEnabled: true,

  /** 自定义队伍列表（当前为空数组） */
  teams: [],

  /** 账号列表，区分不同的大区或子账号 */
  accounts: [
    {
      /** 账号唯一标识 */
      id: "",
      /** 账号创建时间 */
      createdAt: new Date().toISOString()
    }
  ],

  /** 角色分组列表 */
  groups: [
    {
      /** 分组唯一标识（整数值） */
      id: 1,
      /** 分组名称（字符串） */
      name: "分组1",
      /** 分组排序（整数值） */
      sort: 1,
      /** 分组创建时间 */
      createdAt: new Date().toISOString(),
      /** 分组更新时间 */
      updatedAt: new Date().toISOString(),
      /** 每周远征副本通关次数 */
      runs: 0,
      /** 每周超越副本通关次数 */
      transcendRuns: 0,
      /** 分组每日副本通关与收益明细日志（数组结构） */
      runLogs: [
        {
          /** 记录日期（格式：YYYY-MM-DD） */
          date: "",
          /** 副本大类（"expedition" 远征 / "supass" 超越 / "sanctuary" 圣域） */
          type: "expedition",
          /** 当天该类型副本累计通关次数 */
          count: 0,
          /** 当天该类型副本累计获取的基纳收益 */
          kinaGain: 0,
          /** 当天该类型副本累计获取的绑定基纳收益 */
          boundKinaGain: 0,
          /** 创建时间 */
          createdAt: new Date().toISOString(),
          /** 更新时间 */
          updatedAt: new Date().toISOString()
        }
      ]
    }
    // ... 可按需扩展其他分组 (2 ~ 5)
  ],

  /** 角色列表 */
  characters: [
    {
      /** 角色唯一 ID（时间戳格式） */
      id: Date.now(),
      /** 角色系统加密唯一标识符 */
      characterId: "",
      /** 角色游戏昵称 */
      characterName: "",
      /** 角色等级 */
      characterLevel: 1,
      /** 道具/装备评分等级 */
      itemLevel: 0,
      /** 职业名称 */
      className: "",
      /** 性别标识数值 */
      gender: 0,
      /** 性别文本名称 */
      genderName: "",
      /** 角色战力评分 */
      combatPower: 0,
      /** 装备评分 */
      equipmentScore: 0,
      /** 角色所属的分组编号 */
      group: 1,
      /** 角色是否被锁定（防止误操作，通常为 true） */
      locked: true,
      /** 角色是否为主账号（布尔值）用于区分归属于哪个大号或小号 */
      primaryAccount: true,
      /** 角色是否为特级会员（布尔值） */
      premiumMember: false,
      /** 特级会员剩余天数（整数值） */
      premiumMemberDay: 0,
      /** 特级会员开始时间（yyyy-MM-dd 格式） */
      premiumStartTime: "",
      /** 特级会员结束时间（yyyy-MM-dd 格式） */
      premiumEndTime: "",

      // --- 副本与次数进度 ---

      /** 每周远征副本通关次数 */
      runs: 0,
      /** 该角色累计总远征副本通关次数（整数值） */
      totalRuns: 0,
      /** 每周超越副本通关次数 */
      transcendRuns: 0,
      /** 该角色累计总超越副本通关次数（整数值） */
      totalTranscendRuns: 0,

      /** 多 Boss 圣域次数结构（例如 { "s1": 1, "s2": 1, "s3": 1 } 分别对应三个不同 Boss 的进度） */
      sanctuary: {},
      /** 圣域副本次数记录字典（对象结构，按具体副本 key 记录独立次数及每周完成进度） */
      sanctuaryRuns: {},
      /** 圣域副本次数最后更新时间 */
      lastSanctuaryRunsUpdate: "",

      /** 噩梦副本挑战次数 */
      nightmareCount: 0,
      /** 存储的噩梦副本次数 */
      storedNightmareCount: 0,

      /** 每日副本次数（包含 count, maxCount, storedCount 等） */
      dailyRuns: { count: 0, maxCount: 0, storedCount: 0 },
      /** 每日副本次数最后更新时间 */
      lastDailyRunsUpdate: "",
      /** 存储的每日副本次数 */
      storedDailyRuns: 0,

      /** 参与小游戏的次数 */
      minigameCount: 0,
      /** 小游戏次数最后更新时间 */
      lastMinigameUpdate: "",

      /** 单角色每日副本通关与收益明细日志（数组结构） */
      runLogs: [
        {
          date: "",
          type: "expedition",
          count: 0,
          kinaGain: 0,
          boundKinaGain: 0,
          createdAt: "",
          updatedAt: ""
        }
      ],

      // --- 角色状态与养成 ---

      /** 角色是否已觉醒（true / false） */
      awakening: false,
      /** 觉醒的具体时间 */
      awakeningDate: "",

      /** 每日任务完成状态及时间 */
      dailyMission: false,
      dailyMissionDate: "",

      /** 每日签到状态及时间 */
      dailySignIn: false,
      dailySignInDate: "",

      /** 超越玩法相关的备注文本 */
      transcendNote: "",
      /** 当前所属队伍 ID（未组队时为 null） */
      teamId: null,

      /** 当前奥德能量值 */
      energy: 0,
      /** 存储/积压/补充的奥德能量值 */
      storedEnergy: 0,
      /** 本周是否已购买周奥德能量（布尔值） */
      weeklyEnergyPurchased: false,
      weeklyEnergyPurchasedDate: "",
      /** 是否已购买微风奥德能量（布尔值） */
      breezeEnergyPurchased: false,
      breezeEnergyPurchasedDate: "",

      /** 游戏内的吉纳数量（当前角色数据中均为 0） */
      kina: 0
    }
  ],

  // --- 全局/账号共享维度数据拓展 ---

  /** 以太能量商店制作次数（当前已用 usedCount、上限 maxCount: 20、最后更新时间 lastUpdate） */
  aetherEnergyShopCraft: {
    usedCount: 0,
    maxCount: 20,
    lastUpdate: ""
  },

  /** 深渊指令（Abyss Order）在各个账号上的完成状态与时间 */
  abyssOrder: {},

  /** 本地指令（Local Order）在各个账号上的完成状态与时间 */
  localOrder: {},

  /** 按账号统计的小游戏参与进度（包含当前进度 count、最大上限 maxCount、存储次数 storedCount 及 lastUpdate） */
  minigame: {
    count: 0,
    maxCount: 0,
    storedCount: 0,
    lastUpdate: ""
  },

  /** 按账号统计的日常副本完成情况（包含 count、maxCount、storedCount 及 lastUpdate） */
  dailyRuns: {
    count: 0,
    maxCount: 0,
    storedCount: 0,
    lastUpdate: ""
  }
};
```
